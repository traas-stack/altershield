altershield:
  framework:
    server:
      common:
        cloudId: k8s
      group:
        value: altershield
      scheduler:
        testUid: 00
        maxUid: 01

spring:
  datasource:
    jdbc-url: jdbc:mysql://rm-pz5kvz5e5zqm158s3.mysql.rds.aliyuncs.com:3306/altershield?allowPublicKeyRetrieval=true&useSSL=false
    username: altershield
    password: OpsCloud@123

logging:
  config : classpath:log4j2-spring.xml